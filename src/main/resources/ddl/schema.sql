CREATE IF NOT EXISTS TABLE POLLER (
    id INT GENERATED BY DEFAULT AS IDENTITY
            (START WITH 1, INCREMENT BY 1) NOT NULL  PRIMARY KEY,
    name VARCHAR(250),
    url VARCHAR(250),
    status VARCHAR(250) DEFAULT NULL,
);